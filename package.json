{
  "name": "web-video-compressor",
  "version": "1.1.0",
  "description": "Dead-simple ffmpeg wrapper for web video compression",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . 'CompressorFiore' --platform=darwin --arch=arm64 --overwrite --out=dist --icon=assets/icon.icns --ignore=dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^41.1.1",
    "electron-packager": "^17.1.2"
  }
}
